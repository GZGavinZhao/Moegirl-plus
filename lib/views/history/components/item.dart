import 'package:flutter/material.dart';

class HistoryPageItem extends StatelessWidget {
  const HistoryPageItem({Key key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      // child: ,
    );
  }
}